export default function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">brainctl</p>
        <h1>Local dashboard scaffold</h1>
        <p>
          This shell will become the UI for inspecting memory, skills, MCP config, and live agent runs.
        </p>
      </section>
    </main>
  );
}
